#ifndef GAMEMANAGER_H
#define GAMEMANAGER_H

class GameManager {
public:
    // Constructor (opcional, el compilador crea uno por defecto si no lo pones)
    // GameManager(); 

    // Este es el método principal que definiste en el .cpp
    void run();
};

#endif // GAMEMANAGER_H